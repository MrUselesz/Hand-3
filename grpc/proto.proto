syntax = "proto3";
option go_package = "ITUServer/grpc/proto";

message ClientMessage {
    string cmessages = 1;
}

message ServerMessage {
    string smessages = 1;
}

message Empty {}

service ITUDatabase {
    rpc SendRecieve (ClientMessage) returns (ServerMessage) {};
    
}

